//ACTION  -> INCREMENT (describes what you want to do!) it's a simple function

const increment = () => {
	return {
		type: "INCREMENT",
	};
};

const decrement = () => {
	return {
		type: "DECREMENT",
	};
};
